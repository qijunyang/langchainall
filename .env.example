# Where the Ollama server is reachable (matches docker-compose port mapping)
OLLAMA_BASE_URL=http://localhost:11434

# Model to use. Must support tool-calling for the agent demos (Phases 3-4).
# llama3.1 and qwen2.5 both support tools. Pull it with: npm run setup:model
OLLAMA_MODEL=llama3.1
